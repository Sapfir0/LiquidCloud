import { FormControl, Input } from '@material-ui/core';
import React from 'react';

const Login = () => {
    return (
        <FormControl>
            Login
            <Input />
            Password
            <Input />
        </FormControl>
    );
};

export default Login;
